<section class="paper">
  <div class="container-wide inner">
    <blockquote>
      <p class="quote">“Clad accelerated our billing process 10x faster than the way we used to”</p>
      <footer>
        <p class="name">Bob the Builder, COO</p>
        <p class="mono company">BTB &amp; company</p>
      </footer>
    </blockquote>
  </div>
</section>

<style>
  .inner { border-left: var(--hairline) solid var(--color-tar); border-right: var(--hairline) solid var(--color-tar); padding: 91px var(--gutter) 52px; text-align: center; }
  blockquote { margin: 0; }
  .quote { font-size: var(--text-h2); line-height: 1.2; letter-spacing: -1.5px; font-weight: 600; max-width: 860px; margin-inline: auto; }
  footer { margin-top: 35px; }
  .name { font-size: var(--text-body); line-height: 1.3; font-weight: 700; letter-spacing: -0.2px; }
  .company { margin-top: 5px; }
  @media (max-width: 900px) { .inner { padding: 56px var(--gutter) 40px; } }
</style>
